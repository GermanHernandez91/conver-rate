import 'package:flutter/material.dart';

class RatesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Rates"),
      ),
      body: Center(
        child: Text("Rates Screen"),
      ),
    );
  }
}
